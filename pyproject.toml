[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lilbot"
version = "0.2.0"
description = "Minimal local LLM and agent framework with a terminal chat interface"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "Lilbot Contributors" }
]
keywords = ["llm", "agent", "cli", "local-first", "huggingface", "workspace-tools"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Environment :: Console",
  "Intended Audience :: Developers",
]
dependencies = [
  "python-dotenv",
  "psutil",
]

[project.optional-dependencies]
hf = [
  "torch>=2.1",
  "transformers>=4.36",
  "accelerate>=0.26",
]
quantization = [
  "bitsandbytes",
]

[project.scripts]
lilbot = "lilbot.cli.main:main"

[tool.setuptools]
packages = [
  "lilbot",
  "lilbot.cli",
  "lilbot.core",
  "lilbot.llm",
  "lilbot.tools",
]
